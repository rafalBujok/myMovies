import { FavoritePipePipe } from './favorite-pipe.pipe';

describe('FavoritePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FavoritePipePipe();
    expect(pipe).toBeTruthy();
  });
});
